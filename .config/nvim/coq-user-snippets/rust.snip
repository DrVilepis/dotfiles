snippet readints
    #[macro_export]macro_rules!readints{(\$x:ident,\$y:ident)=>{{$x.read_line(&mut\$y).unwrap();let nums=\$y.trim().split(' ').map(|s|s.parse::<i32>().unwrap()).collect::<Vec<_>>();\$y.clear();nums}};}
